{
  "name": "tl-search-for-mam",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite build --watch & vite build --watch --config vite.content.config.ts",
    "build": "vite build && vite build --config vite.content.config.ts && vite build --config vite.guard.config.ts",
    "test": "vitest run",
    "test:watch": "vitest"
  },
  "devDependencies": {
    "@types/chrome": "^0.0.268",
    "jsdom": "^28.1.0",
    "typescript": "^5.3.3",
    "vite": "^5.0.10",
    "vite-plugin-static-copy": "^1.0.2",
    "vitest": "^1.2.0"
  }
}
